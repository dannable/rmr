# Source PDFs — author's IP, kept local
*.pdf

# Generated artifacts
rmfrp.db
.scratch/
*.bak

# Raw PDF dumps for chargen ingest — author's IP, not redistributable.
data/chargen/_raw/

# ERA (Electronic Roleplaying Assistant) source archives — third-party IP.
# Decoded text data lives under data/chargen/professions/ and is committed.
ERA/

# Profession portrait PNGs extracted from the ERA archive — same IP
# umbrella as the .era source.
data/chargen/professions/img/

# Docker compose mounts the SQLite DB into ./db/ on the host
db/

# Secrets / local config
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build / packaging
build/
dist/
*.egg-info/

# Node / Vite (client/)
node_modules/
client/dist/
client/.vite/
*.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
